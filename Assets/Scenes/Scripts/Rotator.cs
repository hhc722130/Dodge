/*using UnityEngine;

public class Rotator : MonoBehaviour
{
    public class Rotator : MonoBehaviour
    {
        public float rotationSpeed =60f;

        void Update()
        {
            // Rotate the object around its local Y axis at the specified speed
            transform.Rotate(0f,  * Time.deltaTime);
        }
    }
}*/
